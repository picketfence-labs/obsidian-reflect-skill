---
name: obsidian-reflect
description: PARA構成のObsidian Vault内で、指定したノート（またはフォルダ）についてArs Contextaの「Reflect」（関連発見・MOC更新の提案）と「Reweave」（既存ノートへの接続追加・claim更新の提案）に相当する処理を行う際に使う。ノートを直接書き換えることはなく、提案をVaultのInboxフォルダに出力するだけ。基本はユーザーが明示的にこのSkillの実行を求めた時に使うが、**Projectの知見を`02-Areas`/`03-Resources`へ記録（ナレッジ化）する直前、および`01-Projects`から`04-Archive`へ移動する直前にも実行することが推奨される**（domainが重なる他ノートの見落としを防ぐチェックとして。既存ノートの自動書き換えは行わない範囲）。
---

# obsidian-reflect

PARA構成のObsidian Vaultに対して、Ars Contextaの6 Rsのうち「Reflect」（関連ノートの見落とし発見・MOC更新提案）と「Reweave」（既存ノートへの接続追加・陳腐化フラグ・claim先鋭化の提案）に相当する処理を行う。**ノートを直接書き換えることは一切なく、提案は必ずInboxフォルダの専用ノートに出力する。**

## 絶対に守ること
- **対象ノートを直接書き換えない**。編集・リネーム・削除は一切行わない
- **git add/commit/pushを行わない**（このSkillの責務ではない。wrapup skill等、別のフローが担う）
- 提案には理由を必ず言語化する（Articulation Test）。根拠のない接続追加・claim変更の提案はしない
- **事実正確性・論理的整合性のファクトチェックはしない**（範囲外）。「矛盾しているように読める」「古い可能性がある」といった言語化可能な兆候の指摘に留め、断定しない
- 変更提案は次の4テストを満たすもののみ採用する（Ars Contexta由来）:
  - **Articulation**: 変更理由を具体的に言語化できるか
  - **Improvement**: その変更で実際にノートが改善するか
  - **Coherence**: 変更後も単一の焦点を保った内容として一貫しているか
  - **Network**: リンク網（トラバース可能性）が改善するか
- claimの無断変更・過度なノート分割・根拠のない接続追加・文体の勝手な書き換えは提案にも含めない

## 事前準備: 設定の読み込み
1. 対象Vaultのルートを確定する。現在の作業repositoryが対象Vaultならそのrootを使う。別のVaultや絶対pathが指定された場合は、その指定を優先する。安全に確定できなければ推測せず確認する
2. `<vault-root>/.agents/skill-config/obsidian-reflect.yaml`を探す。存在すればhost設定として使う
3. host設定が無ければ、このSKILL.mdと同じディレクトリの`config.yaml`を探す。従来のmachine-global installとの互換性のため、存在すれば使う
4. どちらも無ければ`config.yaml.example`をgeneric defaultとして使い、pathの仮定を結果で報告する
5. 読み込んだ設定から以降で使う値を確定する: `vault.root`、`folders.*`（inbox/projects/areas/resources/archive/daily/templates/sources）、`frontmatter.domain_field`、`proposals.output_filename_prefix`、`proposals.archive_subfolder`。`vault.root`が`.`なら対象Vault rootへ解決する

## 手順

### 1. 対象を特定する
- ユーザーの指示から対象ノートパス、または対象フォルダパスを読み取る。パスが無ければ推測せず、ユーザーに確認する
- 相対パスは`vault.root`基準で解決する
- 単一ノートが指定された場合: そのノート1件が処理対象
- フォルダが指定された場合: 直下・再帰配下の`*.md`を対象とする。ただし`folders.templates`配下（テンプレートは知見ノートではない）は除外する。各フォルダの`README.md`はMOCなので、フォルダ一括処理時は暗黙の対象に含めない（明示的にパス指定された場合のみ対象にする）
- 対象ノート数が多い場合（目安: 20件超）は全件を無条件に処理せず、範囲を絞るかどうかユーザーに確認してから進める

### 2. 各対象ノートを読む
- frontmatter全体（`frontmatter.domain_field`の配列、`tags`、既存の`## 関連`セクション等）
- 本文全体
- 本文中の既存`[[wikilink]]`をすべて抽出する（＝そのノートが既に接続済みの相手）。表示名（`|エイリアス`）・見出しアンカー（`#見出し`）は取り除いてノート名だけにする

### 3. 関連候補ノートを探す（Reflect: 検索戦略）
1. **一次絞り込み（`domain`の重なり、機械的）**: Vault全体のfrontmatterを`Grep`し、対象ノートの`domain`配列と1つ以上値が重なるノートを集める。`scripts/domain_index.sh <vault_root> [config.yaml] --note <対象ノートパス>`を使うと、この絞り込みを1コマンドで機械的に実行できる（LLM推論不要。都度Grepの代わりに使ってよい）
2. **二次絞り込み（内容ベース）**: 対象ノートのタイトル・見出し・本文中の固有名詞から検索キーワードを2〜3個選び、Vault全体を`Grep`して1で拾いきれなかった候補を補う。対象フォルダが大きい・候補が絞りにくい場合は`Explore`エージェントに検索を任せてもよい
3. 対象ノート自身と、既にリンク済み（手順2で抽出済み）のノートは候補から除外する。Reflectの目的は「接続の見落とし」の発見であり、既存のリンクを再提案しても意味がない
4. 候補が多い場合は`domain`一致度が高い順に上位10〜15件程度に絞り込む。絞り込みで対象外にした候補があることは、後述の出力の「絞り込みで対象外にした候補」節に明記する（暗黙に切り捨てない）

### 4. 候補ごとに関連性を判断する（Reflectの本体）
- 絞り込んだ候補ノートそれぞれについて、frontmatterと要旨（`## 要約`等の冒頭セクション、無ければ本文冒頭）を読む
- 対象ノートとの関係を評価し、「関連しそう」と判断したものだけ提案化する。関連の種類（同じ`domain`内の別トピック／前提知識／後続の発展／対立する主張、等）を明記する
- 提案の種類:
  - **リンク追加**: 対象ノート（または候補ノート側）に`[[候補ノート]]`への接続を追加する
  - **MOC更新**: 該当フォルダの`README.md`や`_dashboards`相当のMOCに、対象ノートへの参照を追加する
- 各提案は手順「絶対に守ること」の4テストで自己チェックし、通らないものは提案に含めない

### 5. Reweave相当の追加チェック（対象が既存ノートの場合）
- 対象ノートから既にリンクされている先を対象に、以下の**言語化可能な兆候**のみを確認する（ファクトチェックはしない）:
  - **陳腐化・矛盾の兆候**: リンク先ノートの記述が対象ノートの記述と食い違って見えないか、日付・バージョン等から見て古い可能性を示す記述がないか
  - **claim先鋭化の余地**: タイトルや要約文が、ノートの実際の内容を過不足なく言い表せているか（内容の一部しかカバーしていない／逆に内容以上のことを約束していないか）
- 兆候が見つかった場合のみ提案化する。見つからなければこの節は省略してよい

### 6. 機械的チェックを添える（任意・軽量）
- 対象ノート本文中の`[[wikilink]]`について、リンク先が実際にVault内に存在するか確認する（存在しなければ「リンク切れ」として提案に含める）
- 対象ノートへの被リンク数をVault全体から`Grep`で数え、0件であれば「孤立ノート」として言及する
- Vault全体を対象にした同種のチェックが必要な場合は`scripts/find_candidates.sh <vault_root> [config.yaml]`を実行し、その出力（陳腐化候補・リンク切れ・孤立ノートの一覧）を結果に添えてもよい。このスクリプトはLLM推論を使わない機械的チェックであり、実行しなくても本処理は成立する
- Vault全体のdomainグルーピング（Bases/DataviewのKnowledge Map相当）を俯瞰したい場合は`scripts/domain_index.sh <vault_root> [config.yaml]`（引数なし）を実行してもよい。これもLLM推論を使わない機械的チェック

### 7. 提案をMarkdownとして組み立てる
出力フォーマットは下記「出力フォーマット」を参照。対象ノートごとに`## 対象: <ノートパス>`セクションを作り、その下に提案を並べる。提案が1件も無い対象については「見落としている接続・更新の余地は見つからなかった」旨を明記する（黙って省略しない）

### 8. 出力先に書き出す
- ファイルパス: `{vault.root}/{folders.inbox}/{実行日}_{proposals.output_filename_prefix}.md`
  - 実行日は会話コンテキストの現在日付（`YYYY-MM-DD`）を使う
  - 対象がフォルダ一括処理の場合は`_{slug}`を付与する（`slug`は対象フォルダ名から生成。例: `03-Resources/`が対象なら`2026-08-23_reflect-proposals_03-resources.md`）
- 同日・同名の出力ファイルが既に存在する場合は、新しい`## 対象: <パス>`セクションとして追記する（既存内容を上書きしない）
- frontmatterはVaultのInbox規約に合わせる（`tags: [inbox]`、`domain:`は対象ノート・発見した関連ノートで実際に使われている値の和集合、`created:`は実行日）
- **対象ノート自身・MOC（README.md等）は一切書き換えない**。書き換え候補があってもこのファイルへの提案としてのみ記述する

### 9. ユーザーに報告する
- 出力ファイルへのパス、生成した提案の件数、絞り込みで対象外にした候補数（あれば）を報告する
- 「内容を確認し、必要な変更は手動で反映してください」と明記する
- git add/commit/pushは行わない旨、必要であれば改めて触れる
- **提案ファイル自体の後処理についても案内する**: 未処理項目がある間はInboxに保持する。全項目の採否が決まったら、採用／非採用／保留、理由、反映先、検証結果を同じファイルへ追記し、`{folders.archive}/{proposals.archive_subfolder}/`へ移す。削除しない

## 提案処理後の保存（Skill本体とは別フロー）

このSkillは提案生成までを担当し、対象ノートの変更やArchive移動は行わない。利用者または別の作業フローが提案を処理する際は、効果・妥当性を後から評価できるよう次を満たす。

1. 各提案を`採用 | 非採用 | 保留`に分類する。
2. 採用は実施内容と反映先、非採用・保留は理由と、必要なら移管先の未解決タスクを記録する。
3. リンク、frontmatter、対象ノート固有のvalidator等、実施した検証結果を記録する。
4. 全項目の判断と必要な反映が完了した時だけ、提案ファイルを`{folders.archive}/{proposals.archive_subfolder}/`へ移す。未処理項目が残る間はInboxに保持する。
5. 移動前に提案ファイルへの参照を検索し、Archive後のパスへ更新する。

Archiveされた提案はProject／Area／Resourceの正本ではない。Reflect/Reweaveが何を発見し、どの提案が有効だったかを評価する履歴である。未解決の実作業は正本側のタスクへ移管する。

## 出力フォーマット
```markdown
---
tags: [inbox]
domain: [<対象ノート・関連候補で実際に使われているdomain値の和集合>]
created: <YYYY-MM-DD>
---

# Reflect/Reweave提案（<YYYY-MM-DD>）

対象: <ノートパス1>, <ノートパス2>, ...

## 対象: <ノートパス>

### 提案1: Reflect - リンク追加
- **内容**: [[候補ノート]]への接続を追加する（<どちら側から追加するか、あるいは双方向か>）
- **理由（Articulation）**: なぜこの2つが関連するか
- **想定される改善（Improvement）**: 追加することで何が良くなるか
- **影響を受けるノート**: [[対象ノート]], [[候補ノート]]

### 提案2: Reweave - 陳腐化フラグ
- **内容**: ...
- **理由（Articulation）**: 具体的にどの記述が、どう食い違って見えるか
- **影響を受けるノート**: ...

## 機械的チェック結果
- リンク切れ: <あれば列挙。無ければ「なし」>
- 孤立ノート: <あれば列挙。無ければ「なし」>

## 絞り込みで対象外にした候補（参考。あれば）
- <候補ノート名>: <対象外にした理由>

---
*このファイルは`obsidian-reflect` Skillが生成した提案です。内容を確認し、必要な変更は手動でノートに反映してください。ノート本体・git commit/pushへの操作は行っていません。未処理項目がある間はInboxに保持し、全項目の採否・理由・反映先・検証結果を追記した後、設定されたReflect Proposal用Archiveへ移してください。*
```
